# Posterize

Posterize an image with stacked SVG geometry (generated by Potrace).

```python
from posterize import posterize

num_colors = 4

image_approximation = posterize("image.png", num_colors)
image_approximation.write_svg("output.svg")
```
